// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "com.nsvuefire.test"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
